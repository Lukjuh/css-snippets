import styles from "./index.css?inline";

const $default = `<input class="checkbox" type="checkbox" checked>`;

export default {
  html: {
    default: $default
  },
  css: {
    default: styles,
  }
}