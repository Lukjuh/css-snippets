import styles from "./index.css?raw";

const $default = `<label class="label" for="your-input">First name</label>`;

export default {
  html: {
    default: $default
  },
  css: {
    default: styles,
  }
}
