import styles from "./index.css?inline";

const $default = `<label class="label" for="your-input">First name</label>`;

export default {
  html: {
    default: $default
  },
  css: {
    default: styles,
  }
}
