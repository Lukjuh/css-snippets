import styles from "./index.css?inline";

const $default = `<input class="input" type="text" placeholder="First name">`;

export default {
  html: {
    default: $default
  },
  css: {
    default: styles,
  }
}
